package com.audiosub.overlay

// SubtitleView is intentionally unused as a standalone widget —
// the overlay uses overlay_floating.xml inflated directly by SubtitleOverlayManager.
// Kept as a placeholder in case a custom View subclass is needed later.
